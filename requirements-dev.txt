# Development & testing dependencies (not needed at runtime)
# Install with: pip install -r requirements-dev.txt
-r requirements.txt

# Testing
pytest>=8.3,<9
pytest-asyncio>=0.24,<1
respx>=0.21,<1      # HTTPX mocking for tests
